sudo: required

services:
  - docker
  
language: java

before_install:
    - chmod +x mvnw

script:
    mvn test -B

after_success:
  docker login -u $DOCKER_USER -p $DOCKER_PASS
  docker build -f Dockerfile -t $TRAVIS_REPO_SLUG:$Pizz .
  docker push $TRAVIS_REPO_SLUG